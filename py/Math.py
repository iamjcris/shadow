import math
import math as m #can also importing with an alias
from math import sqrt, gcd #can also import specific functions #can also add some functions only

print(math.sqrt(25))
print(m.sqrt(100))
print(sqrt(16))
print(gcd(16))